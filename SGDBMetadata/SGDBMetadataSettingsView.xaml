<UserControl x:Class="SGDBMetadata.SGDBMetadataSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <StackPanel>
        <TextBlock Text="SteamGrid DB API Key"/>
        <TextBox Text="{Binding Option1}"/>
        
        <TextBlock Text="Grid Style"/>
        <ComboBox Name="cmbStyles" ItemsSource="{Binding Style}" SelectedItem="{Binding Path=SStyle}" />
        <TextBlock Text="Grid Dimensions"/>
        <ComboBox Name="cmbDimensions" ItemsSource="{Binding Dimension}" SelectedItem="{Binding Path=SDimension}"/>
    </StackPanel>
</UserControl>