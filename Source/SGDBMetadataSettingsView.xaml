<UserControl x:Class="SGDBMetadata.SGDBMetadataSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <StackPanel>
        <TextBlock Text="API Key:" Margin="15,10,20,0"/>
        <TextBox Text="{Binding Option1}" Margin="15,10,20,0"/>

        <TextBlock Text="Cover Style:" Margin="15,10,20,0"/>
        <ComboBox x:Name="cmbStyles" ItemsSource="{Binding Style}" SelectedItem="{Binding SStyle}" Margin="15,10,20,0" />
        <TextBlock Text="Cover Size:" Margin="15,10,20,0"/>
        <ComboBox x:Name="cmbDimensions" ItemsSource="{Binding Dimension}" SelectedItem="{Binding SDimension}" Margin="15,10,20,0"/>
    </StackPanel>
</UserControl>